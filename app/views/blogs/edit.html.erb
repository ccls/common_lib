<h1>Editing blog</h1>

<%= form_for(@blog) do |f| %>
  <%= f.error_messages %>
  <%= render 'form', :f => f %>
  <p><%= f.submit 'Update' %></p>
<% end %>

<%= link_to 'Show', @blog %> |
<%= link_to 'Back', blogs_path %>
